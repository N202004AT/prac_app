<h1>投稿一覧テスト</h1>
<% @lists.each do |list| %>
	<p>title</p>
	<span>
		<%=link_to list.title, todolist_path(list.id) %>
		</span>
		<p>image</p>
		<%= attachment_image_tag list, :image, :fill,100,100,format: 'jpeg'%>
	<% end %>